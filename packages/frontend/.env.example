# Frontend Environment Variables

# Backend API URL
# Local (requires Docker + backend): http://localhost:4001/api/v1
# Prod (frontend-only dev, no Docker): https://api.foliobuddy.xyz/api/v1
VITE_API_URL=http://localhost:4001/api/v1

# WebSocket Backend URL (Vercel doesn't proxy WebSockets, so connect directly)
# Local dev: http://localhost:4001
# Prod: https://api.foliobuddy.xyz
VITE_WS_BACKEND_URL=http://localhost:4001

# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Sentry Error Tracking (optional - leave empty to disable)
VITE_SENTRY_DSN=
