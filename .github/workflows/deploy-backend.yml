name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'packages/backend/**'
      - 'prisma/**'

jobs:
  deploy:
    name: Deploy to Coolify
    runs-on: ubuntu-latest
    needs: []

    steps:
      - name: Trigger Coolify Deploy
        run: |
          curl -s -X POST \
            "http://private-admin-host:8000/api/v1/applications/app-placeholder/deploy" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"force": true}'
